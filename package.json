{
  "name": "pfa-vercel",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vercel deployment wrapper for the PFA app (frontend + api/ functions).",
  "scripts": {
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10",
    "@vercel/functions": "^1.5.0",
    "groq-sdk": "^0.9.0",
    "mammoth": "^1.8.0",
    "unpdf": "^0.12.1",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.2",
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13",
    "typescript": "^5.6.3"
  }
}
